name: usb_serial
description: USB serial port communication for Flutter on Android.
version: 0.3.0
flutter:
  plugin:
    platforms:
      android:
        package: com.felhr.usbserial
        pluginClass: UsbSerialPlugin
environment:
  sdk: ">=2.17.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
